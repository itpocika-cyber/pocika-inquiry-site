# Backend API Base URL (Render deployment URL)
# In local development, leave unset or set to http://localhost:5000 (or let Vite proxy forward /api)
# In production on Vercel, set this to your Render service URL:
VITE_API_BASE_URL=https://your-pocika-api.onrender.com
